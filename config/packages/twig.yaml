twig:
    default_path: '%kernel.project_dir%/templates'
    globals:
        oling_schema_same_as: '%oling_schema_same_as%'

when@test:
    twig:
        strict_variables: true

framework:
    error_controller: 'App\Controller\ErrorController::error404'
