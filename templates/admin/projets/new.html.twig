{% extends 'base.html.twig' %}

{% block title %}Backoffice - Nouveau projet{% endblock %}
{% block meta_description %}Création de projet Oling{% endblock %}

{% block body %}
<main id="content" role="main" class="admin">
    <div class="container content-space-2 content-space-lg-3">
        <div class="mb-4">
            <h1 class="h3">Nouveau projet</h1>
            <p class="text-muted mb-0">Ajoutez un projet et son image.</p>
        </div>

        {% include 'admin/_nav.html.twig' %}

        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}

        {% include 'admin/projets/_form.html.twig' with {
            form: form,
            projet: projet,
            button_label: 'Créer'
        } %}
    </div>
</main>
{% endblock %}
