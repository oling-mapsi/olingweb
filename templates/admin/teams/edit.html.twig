{% extends 'base.html.twig' %}

{% block title %}Backoffice - Modifier membre{% endblock %}
{% block meta_description %}Modification de membre d'équipe Oling{% endblock %}

{% block body %}
<main id="content" role="main" class="admin">
    <div class="container content-space-2 content-space-lg-3">
        <div class="mb-4">
            <h1 class="h3">Modifier le membre</h1>
            <p class="text-muted mb-0">Mettez à jour les informations et la photo.</p>
        </div>

        {% include 'admin/_nav.html.twig' %}

        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}

        {% include 'admin/teams/_form.html.twig' with {
            form: form,
            team: team,
            button_label: 'Mettre à jour'
        } %}

        <div class="mt-4">
            <form method="post" action="{{ path('admin_teams_delete', {id: team.id}) }}" onsubmit="return confirm('Supprimer ce membre ?');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ team.id) }}">
                <div class="form-check mb-2">
                    <input class="form-check-input" type="checkbox" id="confirm-delete-team-{{ team.id }}" required>
                    <label class="form-check-label" for="confirm-delete-team-{{ team.id }}">
                        Je confirme la suppression.
                    </label>
                </div>
                <button class="btn btn-outline-danger">Supprimer</button>
            </form>
        </div>
    </div>
</main>
{% endblock %}
