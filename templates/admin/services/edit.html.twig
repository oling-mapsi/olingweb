{% extends 'base.html.twig' %}

{% block title %}Backoffice - Modifier service{% endblock %}
{% block meta_description %}Modification de service Oling{% endblock %}

{% block body %}
<main id="content" role="main" class="admin">
    <div class="container content-space-2 content-space-lg-3">
        <div class="mb-4">
            <h1 class="h3">Modifier le service</h1>
            <p class="text-muted mb-0">Mettez à jour les informations et médias.</p>
        </div>

        {% include 'admin/_nav.html.twig' %}

        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">{{ message }}</div>
            {% endfor %}
        {% endfor %}

        {% include 'admin/services/_form.html.twig' with {
            form: form,
            service: service,
            button_label: 'Mettre à jour',
            form_action: path('admin_services_edit', {id: service.id})
        } %}

        <div class="mt-4">
            <form method="post" action="{{ path('admin_services_delete', {id: service.id}) }}" onsubmit="return confirm('Supprimer ce service ?');">
                <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ service.id) }}">
                <div class="form-check mb-2">
                    <input class="form-check-input" type="checkbox" id="confirm-delete-service-{{ service.id }}" required>
                    <label class="form-check-label" for="confirm-delete-service-{{ service.id }}">
                        Je confirme la suppression.
                    </label>
                </div>
                <button class="btn btn-outline-danger">Supprimer</button>
            </form>
        </div>
    </div>
</main>
{% endblock %}
