{{ form_start(form) }}
    <div class="row g-4">
        <div class="col-12">
            <div class="mb-3">
                {{ form_label(form.designation) }}
                {{ form_widget(form.designation, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.designation) }}
            </div>

            <div class="mb-3">
                {{ form_label(form.description) }}
                {{ form_widget(form.description, {'attr': {'class': 'form-control js-wysiwyg'}}) }}
                {{ form_errors(form.description) }}
            </div>

            <div class="mb-3">
                {{ form_label(form.class) }}
                {{ form_widget(form.class, {'attr': {'class': 'form-control'}}) }}
                {{ form_errors(form.class) }}
            </div>

            <div class="mb-3">
                {{ form_label(form.metier) }}
                {{ form_widget(form.metier, {'attr': {'class': 'form-select'}}) }}
                {{ form_errors(form.metier) }}
            </div>

            <div class="mb-3">
                {{ form_label(form.services) }}
                {{ form_widget(form.services, {'attr': {'class': 'form-select'}}) }}
                {{ form_errors(form.services) }}
            </div>

            <div class="form-check mb-3">
                {{ form_widget(form.featuredProjects, {'attr': {'class': 'form-check-input'}}) }}
                {{ form_label(form.featuredProjects, null, {'label_attr': {'class': 'form-check-label'}}) }}
                {{ form_errors(form.featuredProjects) }}
            </div>
        </div>
    </div>

    <div class="row g-4 mt-1">
        <div class="col-12">
            <div class="admin-media-card">
                <div class="admin-media-header">
                    <div>
                        <h5>Image principale</h5>
                        <p>Image standard affichée sur les pages projets.</p>
                    </div>
                </div>
                <div class="admin-media-body">
                    <div class="admin-media-preview">
                        <img class="admin-media-preview__img" src="{{ asset(projet.image ?: 'img/1920x1080/img7.jpg') }}" alt="Image principale">
                    </div>
                    <div class="admin-media-fields">
                        {{ form_label(form.imageFile) }}
                        {{ form_widget(form.imageFile, {'attr': {'class': 'form-control'}}) }}
                        {{ form_errors(form.imageFile) }}
                        <details>
                            <summary>Utiliser une URL</summary>
                            <div class="mt-2">
                                {{ form_label(form.image) }}
                                {{ form_widget(form.image, {'attr': {'class': 'form-control'}}) }}
                                {{ form_errors(form.image) }}
                            </div>
                        </details>
                    </div>
                </div>
            </div>

            <div class="admin-media-card">
                <div class="admin-media-header">
                    <div>
                        <h5>Image recadrée (cards hero)</h5>
                        <p>Recadre la zone qui apparaîtra dans les cartes hero de la home.</p>
                    </div>
                    <button type="button" class="btn btn-outline-secondary btn-sm js-cropper-use-existing">Recadrer l’existante</button>
                </div>
                <div class="admin-media-body">
                    <div class="admin-media-preview">
                        <img class="admin-media-preview__img" src="{{ asset(projet.imageHero ?: projet.image ?: 'img/1920x1080/img7.jpg') }}" alt="Aperçu recadrage">
                    </div>
                    <div class="admin-media-fields">
                        {{ form_label(form.imageHeroFile) }}
                        {{ form_widget(form.imageHeroFile, {'attr': {'class': 'form-control js-cropper-input', 'data-crop-aspect': '0.8'}}) }}
                        {{ form_errors(form.imageHeroFile) }}
                        <details>
                            <summary>Utiliser une URL</summary>
                            <div class="mt-2">
                                {{ form_label(form.imageHero) }}
                                {{ form_widget(form.imageHero, {'attr': {'class': 'form-control'}}) }}
                                {{ form_errors(form.imageHero) }}
                            </div>
                        </details>
                        <div class="admin-cropper" data-input="{{ form.imageHeroFile.vars.id }}">
                            <div class="admin-cropper__header">
                                <div class="admin-cropper__label">Zone de recadrage</div>
                            </div>
                            <div class="admin-cropper__stage">
                                <img class="admin-cropper__image" src="{{ asset(projet.imageHero ?: projet.image ?: 'img/1920x1080/img7.jpg') }}" alt="Aperçu recadrage">
                            </div>
                            <div class="admin-cropper__hint">Glisse et redimensionne la zone puis enregistre.</div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="d-flex gap-2 mt-4">
        <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
        <a class="btn btn-outline-secondary" href="{{ path('admin_projets_index') }}">Retour</a>
    </div>
{{ form_end(form) }}
